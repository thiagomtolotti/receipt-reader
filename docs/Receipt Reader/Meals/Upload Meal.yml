info:
  name: Upload Meal
  type: http
  seq: 1

http:
  method: POST
  url: "{{api_url}}/meal/upload"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
