info:
  name: Ping
  type: http
  seq: 1

http:
  method: GET
  url: "{{api_url}}"
  auth: inherit

runtime:
  variables:
    - name: api_url
      value: http://localhost:8000

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
