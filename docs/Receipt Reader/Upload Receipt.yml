info:
  name: Upload Receipt
  type: http
  seq: 2

http:
  method: POST
  url: "{{api_url}}/receipt"
  body:
    type: multipart-form
    data:
      - name: file
        type: file
        value:
          - /home/thiago/Documents/receipt-reader/backend/samples/nota_sample.png
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
