info:
  name: Upload Receipt
  type: http
  seq: 3

http:
  method: POST
  url: "{{api_url}}/receipt"
  body:
    type: multipart-form
    data:
      - name: file
        type: file
        value:
          - \\wsl.localhost\Ubuntu\home\thiagotolotti\projects\receipt-app\backend\samples\receipt_sample.jpg
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
