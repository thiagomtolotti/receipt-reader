info:
  name: LIst Receipts
  type: http
  seq: 3

http:
  method: GET
  url: "{{api_url}}/receipts"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
